# EV Parking Frontend Environment Configuration
# Copy this file to .env.local and adjust values for your environment

# Backend API Base URL (required)
# Points to your running Node.js/Express backend
VITE_BACKEND_URL=http://localhost:3000/api/v1

# Python ML Model Server URL (optional)
# Used by the Charging Schedule Predictor component
# If not set, the predictor will fail gracefully
VITE_MODEL_URL=http://localhost:5001

# (Optional) Additional configuration below if needed in future

# Debug mode (set to 'true' for verbose logging)
# VITE_DEBUG=false
